<%- include("../partials/headera") %>
<div class="col-lg-6 col-md-12" id="form">
    <div class="row justify-content-center">
        <div class="col-lg-8 col-md-12" style="margin-top: 6%">
            <div id="form1">
                <a href="/services/<%= service.subpageLink %>">
                    <i class="fas fa-arrow-left"></i>
                    Wróć do podstrony o usłudze <%= service.title %>
                </a>
                <h1 class="login">Czy chcesz dodać kolejną podkategorię?</h1>
                <% if(error.length > 0){ %>
                    <div class="alert alert-danger" role="alert">
                        <%= error %>
                    </div>
                <% } %>
                <% if(success.length > 0){ %>
                    <div class="alert alert-success" role="alert">
                        <%= success %>
                    </div>
                <% } %>
                
                    
                <a href="/services/<%= service._id %>/subcategories/add" class="btn btn-primary button btn-golden">Tak</a>
                <a href="/services/<%= service.subpageLink %>" class="btn btn-secondary button">Nie</a>
               
               
            </div>
           
        </div>
        
    </div>
</div>

</body>
</html>